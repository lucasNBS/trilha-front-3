export type ThemeType = {
  fonts: {
    primary: string
    secondary: string
    tertiary: string
  }
  colors: {
    primary: string
    secondary: string
    tertiary: string
    fourthary: string
  }
}
